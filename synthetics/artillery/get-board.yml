config:
  target: "https://trellific.corkboard.dev"
  plugins:
    expect: {}
    publish-metrics:
      - type: mixpanel
        projectToken: "f8b2e48f36b0f64ca4231cc57c71eeaa"
scenarios:
  - name: Get a board
    flow:
    - get:
        url: "/api/boards/62a9fbf7b9e7dbb1bddb0fe2"
        capture:
        - json: "$.title"
          as: title
        expect:
          - statusCode: 200
          - contentType: json
          - hasProperty: title
          - equals:
            - "Test board #1"
            - "{{ title }}"
